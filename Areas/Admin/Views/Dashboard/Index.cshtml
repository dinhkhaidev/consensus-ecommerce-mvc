@{
    var settings = ViewData["WebSettings"] as Dictionary<string, string> ?? new();
    ViewData["Title"] = settings.GetValueOrDefault("AdminDashboardTitle", localizer.Get("Dashboard"));
    var totalProducts = (int)(ViewData["TotalProducts"] ?? 0);
    var totalOrders = (int)(ViewData["TotalOrders"] ?? 0);
    var totalRevenue = (decimal)(ViewData["TotalRevenue"] ?? 0m);
    var pendingOrders = (int)(ViewData["PendingOrders"] ?? 0);
    var totalCustomers = (int)(ViewData["TotalCustomers"] ?? 0);
    var lowStockCount = (int)(ViewData["LowStockCount"] ?? 0);
    var recentOrders = ViewBag.RecentOrders as List<WebActionResults.Data.Entities.Order> ?? new();
    var topProducts = ViewBag.TopProducts as dynamic ?? new List<dynamic>();
}

<div class="row g-3 mb-3 dashboard-stats">
    <!-- Stats Cards -->
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon" style="background: linear-gradient(135deg, #d47f31 0%, #e8a05f 100%);">
                    <i class="bi bi-box-seam text-white"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Total Products</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@totalProducts</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon" style="background: linear-gradient(135deg, #ffc107 0%, #ffdd85 100%);">
                    <i class="bi bi-clock-history text-dark"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Pending Orders</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@pendingOrders</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon" style="background: linear-gradient(135deg, #198754 0%, #4fd98e 100%);">
                    <i class="bi bi-cart-check text-white"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Total Orders</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@totalOrders</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon" style="background: linear-gradient(135deg, #0dcaf0 0%, #7dd3fc 100%);">
                    <i class="bi bi-currency-dollar text-white"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Revenue</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@(((decimal)totalRevenue).ToString("N0"))</h3>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-3 mb-4 dashboard-stats">
    <!-- Second Row Stats -->
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon" style="background: linear-gradient(135deg, #6f42c1 0%, #a78bfa 100%);">
                    <i class="bi bi-people text-white"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Customers</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@totalCustomers</h3>
                </div>
            </div>
        </div>
    </div>
    <div class="col-sm-6 col-xl-3">
        <div class="card border-0 shadow-sm h-100 dashboard-stat-card">
            <div class="card-body d-flex align-items-center gap-3">
                <div class="dashboard-stat-icon @(lowStockCount > 0 ? "bg-danger" : "bg-success")" style="opacity: 0.9;">
                    <i class="bi bi-exclamation-triangle text-white"></i>
                </div>
                <div>
                    <p class="dashboard-stat-label text-muted mb-0 text-uppercase">Low Stock Alert</p>
                    <h3 class="dashboard-stat-value mb-0 fw-bold">@lowStockCount</h3>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row g-4">
    <!-- Recent Orders -->
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm" style="border-radius: 1rem;">
            <div class="card-header bg-white py-3 d-flex justify-content-between align-items-center">
                <h5 class="mb-0"><i class="bi bi-clock-history me-2 text-primary"></i>Recent Orders</h5>
                <a asp-area="Admin" asp-controller="AdminOrder" asp-action="Index" class="btn btn-sm btn-outline-primary">
                    View All <i class="bi bi-arrow-right ms-1"></i>
                </a>
            </div>
            <div class="card-body p-0">
                @if (recentOrders != null && recentOrders.Count > 0)
                {
                    <div class="table-responsive">
                        <table class="table mb-0">
                            <thead class="bg-light">
                                <tr>
                                    <th class="border-0 py-3 ps-4 text-uppercase" style="font-size: 0.7rem;">Order</th>
                                    <th class="border-0 py-3 text-uppercase" style="font-size: 0.7rem;">Customer</th>
                                    <th class="border-0 py-3 text-uppercase" style="font-size: 0.7rem;">Date</th>
                                    <th class="border-0 py-3 text-uppercase" style="font-size: 0.7rem;">Total</th>
                                    <th class="border-0 py-3 text-uppercase" style="font-size: 0.7rem;">Status</th>
                                    <th class="border-0 py-3 pe-4 text-uppercase" style="font-size: 0.7rem;">Actions</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var order in recentOrders)
                                {
                                    <tr class="border-bottom">
                                        <td class="py-3 ps-4">
                                            <span class="fw-semibold text-primary">#@order.Id</span>
                                        </td>
                                        <td class="py-3">@order.User?.UserName</td>
                                        <td class="py-3 text-muted small">@order.CreatedAt.ToString("MMM dd, yyyy")</td>
                                        <td class="py-3 fw-semibold">@order.TotalAmount.ToString("N0") VND</td>
                                        <td class="py-3">
                                            <span class="badge rounded-pill bg-@GetStatusBadge(order.Status)">@order.Status</span>
                                        </td>
                                        <td class="py-3 pe-4">
                                            <a asp-area="Admin" asp-controller="AdminOrder" asp-action="Details" asp-route-id="@order.Id"
                                               class="btn btn-sm btn-outline-primary">
                                                <i class="bi bi-eye"></i>
                                            </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                }
                else
                {
                    <div class="text-center py-5">
                        <i class="bi bi-receipt text-muted" style="font-size: 3rem;"></i>
                        <p class="text-muted mt-2 mb-0">No orders yet</p>
                    </div>
                }
            </div>
        </div>
    </div>

    <!-- Top Products -->
    <div class="col-lg-4">
        <div class="card border-0 shadow-sm" style="border-radius: 1rem;">
            <div class="card-header bg-white py-3">
                <h5 class="mb-0"><i class="bi bi-trophy me-2 text-warning"></i>Top Products</h5>
            </div>
            <div class="card-body p-0">
                @if (topProducts != null && topProducts.Count > 0)
                {
                    <ul class="list-group list-group-flush">
                        @{
                            int rank = 1;
                        }
                        @foreach (dynamic tp in topProducts)
                        {
                            <li class="list-group-item d-flex align-items-center gap-3 py-3 px-3">
                                <div class="rounded-circle bg-primary text-white d-flex align-items-center justify-content-center"
                                     style="width: 32px; height: 32px; font-size: 0.8rem; font-weight: 600;">
                                    @(rank++)
                                </div>
                                <div class="flex-grow-1">
                                    <p class="mb-0 fw-semibold" style="font-size: 0.85rem;">@tp.ProductName</p>
                                    <small class="text-muted">@tp.TotalSold sold</small>
                                </div>
                                <span class="fw-bold text-primary">@(((decimal)tp.TotalRevenue).ToString("N0")) VND</span>
                            </li>
                        }
                    </ul>
                }
                else
                {
                    <div class="text-center py-5">
                        <i class="bi bi-bar-chart text-muted" style="font-size: 3rem;"></i>
                        <p class="text-muted mt-2 mb-0">No sales data yet</p>
                    </div>
                }
            </div>
        </div>
    </div>
</div>

@{
    string GetStatusBadge(WebActionResults.Data.Entities.OrderStatus status) => status switch
    {
        WebActionResults.Data.Entities.OrderStatus.Pending => "warning text-dark",
        WebActionResults.Data.Entities.OrderStatus.Confirmed => "info",
        WebActionResults.Data.Entities.OrderStatus.Processing => "primary",
        WebActionResults.Data.Entities.OrderStatus.Shipped => "info",
        WebActionResults.Data.Entities.OrderStatus.Delivered => "success",
        WebActionResults.Data.Entities.OrderStatus.Cancelled => "danger",
        WebActionResults.Data.Entities.OrderStatus.CancellationRequested => "warning text-dark",
        WebActionResults.Data.Entities.OrderStatus.ReturnRequested => "warning text-dark",
        WebActionResults.Data.Entities.OrderStatus.ReturnApproved => "success",
        WebActionResults.Data.Entities.OrderStatus.ReturnRejected => "danger",
        _ => "secondary"
    };
}

<style>
    .card {
        transition: all 0.3s ease;
    }

    .dashboard-stat-card {
        border-radius: 8px !important;
    }

    .dashboard-stat-card .card-body {
        min-height: 78px;
        padding: 0.9rem 1rem;
    }

    .dashboard-stat-icon {
        width: 40px;
        height: 40px;
        display: grid;
        flex: 0 0 40px;
        place-items: center;
        border-radius: 8px;
    }

    .dashboard-stat-icon i {
        font-size: 1.05rem;
        line-height: 1;
    }

    .dashboard-stat-label {
        font-size: 0.66rem;
        font-weight: 700;
        letter-spacing: 0.04em;
        line-height: 1.2;
    }

    .dashboard-stat-value {
        font-size: 1.22rem;
        line-height: 1.25;
    }

    .card:hover {
        transform: translateY(-2px);
        box-shadow: 0 8px 25px rgba(0,0,0,0.1) !important;
    }

    .rounded-3 {
        border-radius: 0.75rem !important;
    }

    .badge.rounded-pill {
        padding: 0.35em 0.8em;
    }

    .list-group-item {
        border-color: #f0f0f0;
    }
</style>
