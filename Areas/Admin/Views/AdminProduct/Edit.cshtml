@model WebActionResults.Models.Product
@{
    ViewData["Title"] = "Edit Product";
    var categories = ViewBag.Categories as IEnumerable<WebActionResults.Models.Category>;
}

<div class="d-flex align-items-center gap-3 mb-4">
    <a asp-area="Admin" asp-controller="AdminProduct" asp-action="Index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i>
    </a>
    <div>
        <h4 class="mb-0">Edit Product</h4>
        <small class="text-muted">Update product information</small>
    </div>
</div>

<form asp-area="Admin" asp-controller="AdminProduct" asp-action="Edit" method="post">
    <input type="hidden" asp-for="Id" />

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0">Basic Information</h5>
                </div>
                <div class="card-body">
                    <div class="mb-3">
                        <label asp-for="ProductName" class="form-label fw-semibold">Product Name</label>
                        <input asp-for="ProductName" class="form-control form-control-lg" />
                        <span asp-validation-for="ProductName" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="QuantityPerUnit" class="form-label fw-semibold">Description</label>
                        <textarea asp-for="QuantityPerUnit" class="form-control" rows="4"></textarea>
                    </div>

                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label asp-for="UnitPrice" class="form-label fw-semibold">Price (VND)</label>
                            <input asp-for="UnitPrice" class="form-control form-control-lg" type="number" step="1000" />
                            <span asp-validation-for="UnitPrice" class="text-danger"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="form-label fw-semibold">Category</label>
                            <select name="CategoryID" class="form-select form-select-lg">
                                @if (categories != null)
                                {
                                    @foreach (var cat in categories)
                                    {
                                        <option value="@cat.Id" selected="@(Model.CategoryID == cat.Id)">@cat.CategoryName</option>
                                    }
                                }
                            </select>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-lg-4">
            <div class="card border-0 shadow-sm mb-4">
                <div class="card-header bg-white py-3">
                    <h5 class="mb-0">Product Images</h5>
                </div>
                <div class="card-body text-center">
                    <div class="bg-light rounded p-5 mb-3">
                        <i class="bi bi-image text-secondary" style="font-size: 3rem;"></i>
                        <p class="text-muted small mt-2 mb-0">Product images can be managed later</p>
                    </div>
                </div>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary btn-lg flex-grow-1">
                    <i class="bi bi-check-lg me-2"></i>Save Changes
                </button>
                <a asp-area="Admin" asp-controller="AdminProduct" asp-action="Index" class="btn btn-outline-secondary btn-lg">
                    Cancel
                </a>
            </div>
        </div>
    </div>
</form>

<style>
    .card {
        border-radius: 1rem;
    }
</style>