@model Dictionary<string, string>
@{
    var settings = Model;
}
@*
    This view component exposes WebSettings globally.
    It's rendered in _Layout.cshtml and makes settings available via ViewData["WebSettings"]
*@